package com.designpattern.builder;

import com.designpattern.entity.Base;

public class FooComponent extends Base {
    public FooComponent(Long id, String description) {
        super(id, description);
    }
}
